import { css } from '@emotion/core';
import { padding } from '../../../../utility/mixin';

const main = css`
  ${padding()};

  h1 {
    font-size: 3rem;
    font-weight: 500;
  }
`;

export default main;
