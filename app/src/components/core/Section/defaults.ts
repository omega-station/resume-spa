import { Props } from './definition';
import { section } from '../../../utility/constant';

const defaults: Props = {
  pagetype: 0,
  section: section[0],
  isWaypoint: false,
  hasEnhancedHeading: false,
  hasListItemCheck: false,
  hasResumeImage: false,
  useSkillsetChart: false,
};

export default defaults;
